function MenuCustomers_Click() {
  // Assuming `UnloadMe` corresponds to hiding or closing a form
  document.getElementById("yourFormId").style.display = "none"; // Hide the form
  // Making the main application visible
  document.getElementById("mainApplicationId").style.display = "block"; // Show the main application
}
